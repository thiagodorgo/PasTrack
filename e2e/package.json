{
  "name": "pastrack-e2e",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Testes ponta a ponta do PasTrack (Playwright contra o Docker Compose)",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "instalar-navegador": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^20.19.43",
    "typescript": "~5.9.3"
  }
}
